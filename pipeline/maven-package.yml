---
platform: linux

image_resource:
  type: docker-image
    image_resource:
    type: docker-image
    source: {repository: ubuntu, tag: "14.04"}

inputs:
- name: git-repo

outputs:
- name: jar-file


run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]
