---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: 2.3.0

inputs:
- name: git-repo

outputs:
- name: jar-file


run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]
